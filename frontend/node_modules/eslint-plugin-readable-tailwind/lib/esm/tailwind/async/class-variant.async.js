import { runAsWorker } from "synckit";
let getClassVariantModule;
runAsWorker(async (version, request) => {
    getClassVariantModule ?? (getClassVariantModule = await import(`../v${version}/class-variant.js`));
    return getClassVariantModule.getClassVariant(request);
});
//# sourceMappingURL=class-variant.async.js.map