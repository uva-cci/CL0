import { runAsWorker } from "synckit";
let getUnregisteredClassesModule;
runAsWorker(async (version, request) => {
    getUnregisteredClassesModule ?? (getUnregisteredClassesModule = await import(`../v${version}/unregistered-classes.js`));
    return getUnregisteredClassesModule.getUnregisteredClasses(request);
});
//# sourceMappingURL=unregistered-classes.async.js.map